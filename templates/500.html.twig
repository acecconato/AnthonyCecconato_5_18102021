{% extends 'base.html.twig' %}

{% block body %}
    <h1>500: Erreur serveur</h1>
    {% if isDevMode %}
        <p>{{ dump(exception) }}</p>
{#        <h2>Backtrace:</h2>#}
{#        {% for trace in backtrace %}#}
{#            {{ dump(trace) }}#}
{#        {% endfor %}#}
    {% endif %}
{% endblock %}